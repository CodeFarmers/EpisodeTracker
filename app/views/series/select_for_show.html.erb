<div class="hero-unit">
  <h2>Your search returned the following results:</h2><br />
  <% @remote_series.each do |key, series| %>
      <h4><%= series[:series_name]%></h4>
      <h6><%= series[:series_overview] %></h6>
      <br/>
  <% end %>
</div>