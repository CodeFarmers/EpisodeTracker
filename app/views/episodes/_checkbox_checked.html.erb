<%= form_for(:user_episode, url: "/user_episodes/#{episode_id}", method: "delete", remote: true ) do |f| %>
    <%= check_box_tag(:user_episode, episode_id, true, { :class => "submittable"} ) %>
<% end %>