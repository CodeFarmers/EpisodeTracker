<%= form_for(:user_episode, url: "/user_episodes/#{user_episode.id}", method: "delete", remote: true ) do |f| %>
    <%= check_box_tag(:episode, episode_id, true, { :class => "submittable"} ) %>
<% end %>